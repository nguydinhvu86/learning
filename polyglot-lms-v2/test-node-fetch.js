const fetch = require('node-fetch');

async function test() {
  try {
    // We already know block '05287b0d' is VOCABULARY with seq_no=1 from earlier
    const id1 = '05287b0d'; // Truncated but whatever, we can just fetch all blocks to get a real ID!
  } catch (e) {}
}
